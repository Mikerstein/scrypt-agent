{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
```

**2. Make sure DB_CONNECTION is set to pgsql**

In Railway → scrypt-agent → Variables, confirm you have:
```
DB_CONNECTION=pgsql
DATABASE_URL=postgresql://postgres:PASSWORD@interchange.proxy.rlwy.net:44461/railway